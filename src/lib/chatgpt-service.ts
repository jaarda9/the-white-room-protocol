/**
 * @deprecated Import from `@/lib/ai-gateway-client` (`aiGatewayClient`, `complete`, `completeJson`).
 */
export {
  aiGatewayClient,
  aiGatewayClient as default,
  chatGPTService,
} from './ai-gateway-client';
